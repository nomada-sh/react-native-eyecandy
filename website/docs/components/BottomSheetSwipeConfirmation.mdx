---
id: bottomsheetswipeconfirmation
title: BottomSheetSwipeConfirmation
---

BottomSheetSwipeConfirmations are used to add a confirmation to any action in your app that is invoked with a [ButtonSwipe](buttonswipe). This is useful for things like deleting items or taking some other destructive action. 

## Import

```jsx
import { BottomSheetSwipeConfirmation } from '@nomada-sh/react-native-eyecandy';
```

## Example

```tsx
import React from 'react';
import { Alert, ScrollView } from 'react-native';

import {
  BottomSheetSwipeConfirmation,
  Button,
} from '@nomada-sh/react-native-eyecandy';

export default function App() {
  const [visible, setVisible] = React.useState(false);

  return (
    <ScrollView
      contentContainerStyle={{
        padding: 20,
      }}
    >
      <Button onPress={() => setVisible(true)} text="Show swipe confirmation" />
      <BottomSheetSwipeConfirmation
        title="Swipe to confirm"
        visible={visible}
        onClose={() => setVisible(false)}
        onConfirm={() => {
          Alert.alert('Swipe confirmed');
          setVisible(false);
        }}
        swipeTitle="Swipe me"
      />
    </ScrollView>
  );
}
```

![BottomSheetSwipeConfirmation](../assets/components/bottomsheetswipeconfirmation.gif)

## Props

### [BottomSheet Props](bottomSheet#props)

Inherits [BottomSheet Props](bottomSheet#props)

---

### <div class="label required basic">Required</div>**`title`**

| Type   |
| ------ |
| string |

---

### `swipeTitle`

| Type   |
| ------ |
| string |

---

### `onConfirm`

```jsx
() => void;
```

| Type     |
| -------- |
| function |

---

### `style`

| Type       |
| ---------- |
| View Style |
