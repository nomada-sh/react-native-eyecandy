import type {
  ThemeButton,
  ThemeText,
  ThemeInput,
  ThemeSwitch,
} from './components';

export type ThemeComponents = {
  button: ThemeButton;
  input: ThemeInput;
  text: ThemeText;
  toggle: ThemeSwitch;
};
